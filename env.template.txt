# ==========================================
# ONDEON SMART - Variables de Entorno
# ==========================================
# 
# INSTRUCCIONES:
# 1. Copia este archivo a `.env` en la raíz del proyecto
# 2. Configura los valores según tu entorno
# 3. NO commits el archivo `.env` (está en .gitignore)
#
# COMANDO RÁPIDO:
#   cp env.template.txt .env
#
# ==========================================

# CloudFront Domain (OBLIGATORIO para producción)
# Este dominio se usa para servir archivos de audio/video desde CloudFront
# en lugar de acceder directamente a S3 (que devuelve 403 Forbidden)
VITE_CLOUDFRONT_DOMAIN=d2ozw1d1zbl64l.cloudfront.net

# Supabase Configuration (configurar según tu instancia)
# Estas variables deben configurarse en cada instalación
# VITE_SUPABASE_URL=https://tu-proyecto.supabase.co
# VITE_SUPABASE_ANON_KEY=tu-clave-anonima-aqui

# ==========================================
# NOTAS IMPORTANTES:
# ==========================================
#
# 1. Variables que empiezan con VITE_ están disponibles en el cliente
# 2. Las variables se reemplazan en tiempo de BUILD, no de runtime
# 3. Después de modificar este archivo, ejecuta: npm run build
# 4. Para producción, asegúrate de tener estas variables configuradas
#    ANTES de ejecutar electron:build
#
# ==========================================




